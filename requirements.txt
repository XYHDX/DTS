# DamascusTransit — production dependencies
#
# Constraints relaxed to "minimum that works" with open upper bounds so pip
# always finds something on PyPI. Several previous pins (fastapi>=0.136.0,
# pydantic>=2.13.2, python-dotenv>=1.2.2, PyJWT==2.12.1) reference future
# versions that aren't published yet, causing Vercel cold-start crashes.
#
# When you're ready to lock to specific versions for reproducibility, generate
# a constraints file from a working `pip freeze` and commit it as
# requirements.lock.txt.

fastapi>=0.110.0
httpx>=0.27.0
PyJWT>=2.8.0
bcrypt>=4.0.0
pydantic>=2.5.0
python-dotenv>=1.2.2
upstash-redis>=1.0.0
gtfs-realtime-bindings>=1.0.0
protobuf>=4.21.0
sentry-sdk[fastapi]>=2.0.0
pywebpush>=2.0.0
