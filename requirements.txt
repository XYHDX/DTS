# DamascusTransit — production dependencies
#
# Minor versions refreshed 2026-05-24 (Phase A revival).
# Major version bumps (bcrypt 5.x, gtfs-realtime-bindings 2.x) are deferred to
# a separate feature branch with a full pytest run — see ROADMAP_100.md step 71.

fastapi>=0.136.0,<0.140.0
httpx==0.28.1
PyJWT==2.12.1
bcrypt==4.2.1                       # 5.x bumps on its own branch
pydantic>=2.13.2,<2.20.0
python-dotenv>=1.2.2
pytest-timeout>=2.3.12
upstash-redis>=1.7.0,<2.0.0
gtfs-realtime-bindings>=1.0.0,<2.0.0  # 2.0 on its own branch
protobuf>=4.25.0
sentry-sdk[fastapi]>=2.58.0,<3.0.0
pywebpush>=2.3.0

# Scale track (Scale_100k_Roadmap.md)
aiomqtt>=2.0.0,<3.0.0                  # S2.2 — async MQTT consumer (api/workers/mqtt_consumer.py)
prometheus-fastapi-instrumentator>=7.0.0  # S7.2 — optional /metrics (enable with METRICS_ENABLED=true)
