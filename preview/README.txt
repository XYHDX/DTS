DamascusTransit — Visual Identity preview
=========================================

Open visual-identity.html in your browser
(double-click works on macOS; nothing else needed).

This folder is a self-contained snapshot of /source/public/visual-identity.html
plus the assets it references — kept separate from the production source so it
can be opened locally via file:// without a web server.

The canonical file the deploy serves is still
  source/public/visual-identity.html
This is just a local-preview copy.
