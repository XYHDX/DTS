feat(wave-N): full E2E test sweep + phases A-G (resilience, search, ETA, photos, tests)

Run a thorough Chrome-MCP sweep against prod (dts-brown.vercel.app):
the v5.0 code is deployed but several wave-6 commits + 7 Supabase
migrations are still pending. 13 of 19 tested features map back to
"run the migrations" or "push the latest". Plus one real new bug
(enum-resilience regex) found and fixed.

PHASE A — paste-once migration bundle + walkthrough
  • source/db/MIGRATIONS_BUNDLE.sql (~33 KB, 011→017 concatenated).
  • MIGRATIONS_WALKTHROUGH.md with verification queries.

PHASE B — fix enum-resilience regex (the one real new bug)
  • _INVALID_ENUM_RE updated to tolerate JSON-escaped quotes.
  • New _decode_body helper unescapes raw response bodies before
    matching, so `\"dispatched\"` is now captured correctly.
  • Verified against the exact prod 502 body shape.

PHASE C — passenger search submit fix
  • Search form had an unguarded <button type="submit"> that reloaded
    the page on Enter, wiping the autocomplete state.
  • Added form.submit handler that picks the highlighted item (or the
    single match) and otherwise no-ops.

PHASE D — passenger route-detail with live ETA
  • /passenger/?route=R101 now shows a card above the popular-routes
    panel listing each stop with sequence, distance, and eta_min from
    /api/routes/{code}/stops.
  • Auto-refresh every 20 s. Bilingual EN/AR.
  • Single-screen; back-button returns to the homepage.

PHASE E — incident photo upload to Supabase Storage
  • New migration 018_incident_photos_bucket.sql creates the private
    "incidents" bucket (5 MB cap, JPG/PNG/WEBP only) with RLS
    policies that scope writes per-driver and reads to dispatcher+.
  • New alerts.photo_path column.
  • POST /api/driver/incident_upload_url returns a signed 5-min PUT URL.
  • POST /api/driver/incident now accepts photo_path and validates the
    first segment matches the driver's user_id.
  • GET /api/admin/alerts/{id}/photo mints a 5-min signed GET URL,
    operator-scoped.
  • Driver app rewired: capture → sign → PUT → create alert. Bilingual
    confirmation message.

PHASE F — automated tests for the new endpoints
  • New tests/test_wave6_endpoints.py covers auth gates, body
    validation, status-transition guards, resilience helpers, CSV
    export, driver endpoints — without needing a live Supabase
    (uses monkey-patched _sb_* helpers).
  • 25 tests across 7 classes. Parses cleanly with `python -c
    "import ast; ast.parse(...)"`.

PHASE G — stale branch cleanup helper
  • New CLEANUP_BRANCHES.command lists obsolete remote branches and
    deletes them with a confirmation prompt. Editable safelist.

API endpoint count: 59 (was 57 at end of wave 6).
